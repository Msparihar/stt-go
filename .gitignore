# Binaries
stt-go.exe
stt-go.exe~
stt-go-old.exe
*.exe

# Build artifacts
build_err.log
*err.log

# Runtime files (config.json contains API keys; logs may contain transcripts)
config.json
stt-go.log
stt-go.log.*
stt-go-*.log.gz
*.log.gz
stderr.log
mismatches.jsonl

# Audio captures (may contain sensitive user audio)
debug-audio/
failed-audio/

# Local Whisper sidecar (venv, model cache, runtime log — never commit)
sidecar/.venv/
sidecar/models/
sidecar/server.out.log
sidecar/__pycache__/

# Environment files (NEVER commit — secrets)
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
